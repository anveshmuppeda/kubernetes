const sidebars = {
  commandsSidebar: [
    {
      type: 'doc',
      id: 'commands', // This should match the filename (without extension) in the `commands/` directory
      label: 'Commands Overview',
    },
  ],
};

module.exports = sidebars;